# 手机端独立运行打包与页面适配实施方案

> **文档定位**：本项目（无情AI助手）从纯前端 Web 端迁移为**手机端独立原生 App（Android APK）**的完整技术实施方案、页面适配清单与进度接力看板。
> **目标**：不依赖任何外部服务器，完全在手机本地运行，离线可用，原生体验。

---

## 一、 项目进度追踪看板（接力用）

> **说明**：每次执行新步骤或完成某个阶段，更新本章节的状态标记（`[ ]` 待开始、`[>]` 进行中、`[x]` 已完成）。

| 阶段 | 任务模块 | 细分任务 | 状态 | 责任人 / 备注 |
| :--- | :--- | :--- | :---: | :--- |
| **阶段一：环境与打包** | 1. 独立 JDK 准备 | 下载/解压免安装 OpenJDK 21（匹配 Capacitor 7 规范，不破坏全局 Java 8） | `[x]` | 已就绪 (`D:\work\Java\jdk-21`) |
| | 2. Capacitor 初始化 | 安装核心依赖、生成 `capacitor.config.json` 与 `sync-assets.js` | `[x]` | 已完成 |
| | 3. Android 工程对接 | 生成 `android/` 目录并绑定 `D:\work\Android_Sdk` | `[x]` | 已完成 |
| | 4. 原生网络与权限 | 配置 AndroidManifest（明文 HTTP、adjustResize、文件读写） | `[x]` | 已完成 |
| | 5. 首次出包与一键脚本 | 根目录创建 `build-apk.bat`，自动编译并输出 `智能摸鱼.apk` 到根目录 | `[x]` | 已完成并验证 |
| **阶段二：页面交互适配** | 6. 顶栏安全区与角色区 | 顶栏避让汉堡按钮，修复 `.persona-combo` 弹性挤压丢失，备忘录/Tokens自适应 | `[x]` | 已落地 |
| | 7. 主侧边栏抽屉化 | 真实遮罩层 `#sidebar-backdrop`、显式关闭按钮，触屏彻底禁用悬浮卡片 | `[x]` | 已落地 |
| | 8. 底部输入栏防溢出 | 输入框独立首行，工具按钮与发送按钮分行整齐排列，气泡统计防溢出 | `[x]` | 已落地 |
| | 9. 视口与键盘适配 | 引入 `100dvh` / `visualViewport`，解决输入法顶起错位与滚动跟随 | `[ ]` | 下一批待执行 |
| | 10. 弹窗与设置抽屉化 | 弹窗移动端全屏/半屏抽屉适配，修复背景滚动穿透（Scroll Locking） | `[ ]` | 下一批待执行 |
| | 11. 代码预览抽屉化 | 右侧代码预览栏改为移动端全屏/半屏抽屉交互 | `[ ]` | 下一批待执行 |
| | 12. 双模式主题校验 | 检查深色/浅色模式在移动端的状态栏与界面色彩 | `[ ]` | 持续校验中 |
| **阶段三：功能与稳定性** | 13. 本地存储验证 | 验证 IndexedDB 和 LocalStorage 在 App 沙盒的持久性 | `[ ]` | 待执行 |
| | 14. 文件与文档解析 | 验证手机端本地上传 txt/pdf/docx/xlsx 的前端解析 | `[ ]` | 待执行 |
| | 15. 流式响应与滚动 | 验证真机上 SSE 流式输出跟随与手势中断锁定的流畅度 | `[ ]` | 待执行 |

---

## 二、 现有资产与打包流水线（基于 D:\work）

### 2.1 现有工具资产与隔离策略

- **Node.js**：`v25.2.1` / npm `11.6.0`（系统已就绪）
- **Android SDK**：`D:\work\Android_Sdk`（包含 build-tools 36.1.0/35.0.0、platforms 36）
- **Java 隔离策略**：系统全局 `JAVA_HOME` 维持 `D:\work\Java\jdk1.8.0_131` 不动。本项目构建时通过 `android/gradle.properties` 单独指定便携式 `OpenJDK 21`，完全隔离互不影响。

```properties
# android/gradle.properties 中指定 Gradle 运行时 JDK
org.gradle.java.home=D:\\work\\Java\\jdk-21
```
```properties
# android/local.properties 中指定 Android SDK
sdk.dir=D:\\work\\Android_Sdk
```

---

### 2.2 打包工具模块化目录结构（手机端打包工具/）

为保持根目录整洁，所有打包相关脚本和配置文件已归拢至 `手机端打包工具/` 独立目录中：

```
ruthless-ai-assistant/
├── 手机端打包工具/
│   ├── capacitor.config.json    # Capacitor 容器配置文件
│   ├── sync-assets.js           # 静态资源抽取同步脚本
│   ├── build-apk.ps1            # 核心自动化编译流水线
│   └── 打包手机APK.bat          # 目录内直接启动脚本
├── 一键打包手机APK.bat           # 根目录一键启动快捷脚本
└── 智能摸鱼.apk                 # 编译完成后的最终安装包
```

#### 两种运行方式（任选其一）：
1. **方式一（推荐）**：直接在根目录下双击 **`一键打包手机APK.bat`**。
2. **方式二**：进入 `手机端打包工具/` 目录，双击 **`打包手机APK.bat`**。

#### 自动化打包流水线：
- 自动提取前端核心静态资源至 `www/`。
- 自动同步至 Android 原生工程（`npx cap sync android`）。
- 自动调用 `D:\work\Java\jdk-21` 运行 Gradle 进行 APK 编译。
- 自动将生成的最新安装包输出到根目录，重命名为 **`智能摸鱼.apk`**。

---

### 2.3 原生配置与关键权限清单

在 `android/app/src/main/AndroidManifest.xml` 中已完成以下配置：

1. **允许明文 HTTP 请求**（用于连接局域网本地模型，如 `http://192.168.x.x:11434`）：
   ```xml
   <application
       android:usesCleartextTraffic="true"
       ... >
   ```
2. **硬件加速与软键盘自适应**：
   ```xml
   <activity
       android:windowSoftInputMode="adjustResize"
       android:hardwareAccelerated="true"
       ... >
   ```
3. **网络与文件读取权限**：
   ```xml
   <uses-permission android:name="android.permission.INTERNET" />
   <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
   ```

---

## 三、 移动端 UI & 交互适配改造清单

> **设计原则**：严格遵循纯原生 CSS3 响应式规范，保持深色/浅色双模式完美适配，严禁破坏原有 PC 端的布局逻辑。

### 3.1 顶栏与角色选择器适配
- **汉堡按钮避让**：`.persona-bar` 左侧设置 `padding-left: 48px` 及顶部安全区，杜绝重叠。
- **角色选择器防挤压**：`.persona-combo` 强制声明 `flex-shrink: 0 !important; min-width: 120px;`，防止在弹性布局中被挤压至 0 宽度消失。

### 3.2 会话侧边栏抽屉化 (Sidebar Drawer)
- **抽屉模式**：`@media (max-width: 768px)` 下，侧边栏变为 `position: fixed; width: 82%; z-index: 150;`。
- **实体遮罩层**：新增 `#sidebar-backdrop`，点击背景空白处或侧边栏头部 `×` 按钮顺畅收起。
- **触屏预览卡片静默**：移动端检测到触控操作时彻底禁用 `mouseenter` 悬浮预览，避免遮挡阻碍操作。

### 3.3 底部输入栏防溢出与排版
- **两行自适应排版**：输入框独占第一行，第二行平铺工具按钮并右浮动发送按钮（✈），彻底解决 8 个按钮挤单行溢出问题。
- **消息气泡统计防溢出**：`.message-footer` 与 `.message-stats` 支持 `flex-wrap: wrap`，字号调至 `0.65rem`，避免横向顶穿气泡边界。

### 3.4 视口高度与软键盘适配 (待推进)
- 主容器高度使用 `100dvh`，键盘弹起时配合 `visualViewport` 确保输入框不被遮挡。

### 3.5 弹窗与代码预览抽屉化 (待推进)
- 弹窗移动端 95% 居中，增加背景滚动锁定（Scroll Lock）。
- 代码预览栏由右侧并排改为底部滑出式抽屉。

---

## 四、 验证与测试用例

| 编号 | 测试分类 | 验证操作 | 预期结果 |
| :---: | :--- | :--- | :--- |
| **TC-01** | 一键打包 | 双击运行根目录 `build-apk.bat` | 顺利编译并生成根目录 `智能摸鱼.apk` |
| **TC-02** | 离线安装 | 手机安装 APK 并断开电脑连接启动 | 启动页无白屏，秒进聊天主界面 |
| **TC-03** | 顶栏角色 | 查看顶栏角色选择器与备忘录 | 角色下拉框清晰可见，点击能正常切换角色 |
| **TC-04** | 侧边栏抽屉 | 点击汉堡按钮呼出侧边栏，点击外部遮罩 | 侧边栏流畅滑出，点击遮罩平滑收起 |
| **TC-05** | 会话无卡片 | 在手机端轻触/长按历史会话 | 不再弹出碍眼的悬浮预览卡片，直接切入会话 |
| **TC-06** | 气泡底栏 | 发送消息查看 AI 回复底部的统计信息 | 字符数、tokens、耗时整齐排列在气泡内不溢出 |
| **TC-07** | 输入与发送 | 底部输入框输入文字并点击发送 | 打字视野开阔，发送按钮灵敏好按 |
| **TC-08** | 本地存储 | 添加自定义角色、修改主题后杀进程重进 | 配置、历史会话与头像完好无损保留 |
