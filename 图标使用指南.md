# 图标使用指南 (Icon Usage Guide)

> 最后更新：2026-03-25

## 一、统一规范

本项目**统一使用 Lucide/Feather 风格 SVG 图标**，具有以下特征：

| 属性 | 标准值 |
|------|--------|
| `viewBox` | `0 0 24 24` |
| `fill` | `none` |
| `stroke` | `currentColor` |
| `stroke-width` | `2` |
| `stroke-linecap` | `round` |
| `stroke-linejoin` | `round` |

### 标准模板

```html
<svg xmlns="http://www.w3.org/2000/svg" width="SIZE" height="SIZE"
     viewBox="0 0 24 24" fill="none" stroke="currentColor"
     stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
    <!-- path 内容 -->
</svg>
```

### 尺寸规范

| 使用场景 | width/height | 说明 |
|----------|:------------:|------|
| `persona-bar` 工具按钮 | `16` | 信息按钮、全屏按钮等 |
| 侧边栏视图切换 | `18` | 列表/日历/删除等 |
| 输入区操作按钮 | `20` | 附件、发送、快捷提示等 |
| 侧边栏切换(汉堡菜单) | `28` | 较大的触控区域 |
| 代码预览面板 | `20`-`24` | 滚动/关闭按钮 |

---

## 二、图标来源

推荐从以下网站获取（下载后使用网址不依赖 CDN）：

- **Lucide Icons**: https://lucide.dev/icons
- **Feather Icons**: https://feathericons.com

> 两者风格完全一致（Lucide 是 Feather 的活跃分支），直接复制 SVG 代码即可。

---

## 三、现有图标清单

### ✅ 已统一（Lucide/Feather 风格）

| 位置 | 按钮 | 图标名称 |
|------|------|----------|
| `sidebar.html` | 简洁列表 | `list` |
| `sidebar.html` | 时间分组 | `calendar` |
| `sidebar.html` | 批量删除 | `trash-2` |
| `index.html` | 侧边栏切换 | `menu`（hamburger） |
| `index.html` | 更新日志 | `clipboard-plus` |
| `index.html` | 系统信息 | `info`（circle） |
| `index.html` | 代码预览滚动 | `chevron-up` / `chevron-down` |
| `index.html` | 代码预览关闭 | `x` |

### ⚠️ 待统一（非标准风格）

| 位置 | 按钮 | 当前风格 | 问题 |
|------|------|----------|------|
| `sidebar.html:6` | 隐藏与总结 | Bootstrap Icons (`viewBox 0 0 16 16`, `fill`) | 应改为 Lucide `eye` |
| `sidebar.html:13` | 设置齿轮 | **iconfont** (`viewBox 0 0 1024 1024`) | 应改为 Lucide `settings` |
| `sidebar.html:23` | 新建对话 | Phosphor Icons (`viewBox 0 0 256 256`) | 应改为 Lucide `plus-circle` |
| `index.html:43` | 退出全屏 | Bootstrap Icons 24x24 fill | 应改为 Lucide `minimize-2` |
| `index.html:73` | 分支左箭头 | Bootstrap Icons 16x16 fill | 应改为 Lucide `chevron-left` |
| `index.html:84` | 分支右箭头 | Bootstrap Icons 16x16 fill | 应改为 Lucide `chevron-right` |
| `index.html:105` | 搜索 | Bootstrap Icons 16x16 fill | 应改为 Lucide `search` |
| `index.html:123` | 搜索关闭 | Bootstrap Icons 16x16 fill | 应改为 Lucide `x` |
| `index.html:140` | 全屏 | Bootstrap Icons 16x16 fill | 应改为 Lucide `maximize-2` |
| `index.html:156` | 滚动到顶部 | Bootstrap Icons 16x16 fill | 应改为 Lucide `chevron-up` |
| `index.html:163` | 滚动到底部 | Bootstrap Icons 16x16 fill | 应改为 Lucide `chevron-down` |
| `index.html:171` | 楼层跳转 | Bootstrap Icons 16x16 fill | 应改为 Lucide `archive` |
| `index.html:194` | 选择数据库 | 自定义 SVG fill:none | 保持即可（已是 stroke 风格） |
| `index.html:203` | 选择表 | 自定义 SVG fill:none | 保持即可（已是 stroke 风格） |
| `index.html:214` | 附件 | Bootstrap Icons 16x16 fill | 应改为 Lucide `paperclip` |
| `index.html:222` | 快捷提示 | Bootstrap Icons 16x16 fill | 应改为 Lucide `zap` |
| `index.html:231` | MCP工具 | Bootstrap Icons 16x16 fill | 应改为 Lucide `terminal` |
| `index.html:241` | 清空输入 | Bootstrap Icons 16x16 fill | 应改为 Lucide `x` |
| `index.html:248` | 发送 | Bootstrap Icons 16x16 fill | 应改为 Lucide `send` |

### 📝 JS 动态生成图标

以下 JS 文件中包含动态生成的 SVG 图标,后续统一时也需要一并处理：

| 文件 | 用途 |
|------|------|
| `js/renderer.js` | 消息操作按钮（编辑、删除、复制等） |
| `js/message-manager.js` | 消息管理操作图标 |
| `js/code-block-enhancer.js` | 代码块工具栏（复制、折叠、预览等） |
| `js/sidebar.js` | 会话列表操作图标（置顶、删除、重命名等） |
| `js/floating-button.js` | 浮动按钮图标 |
| `js/attachment.js` | 附件操作图标 |
| `js/quick-prompts.js` | 快捷提示列表图标 |
| `js/code-preview-manager.js` | 代码预览工具栏图标 |
| `js/mcp-management.js` | MCP工具管理图标 |
| `js/ui-updater.js` | UI状态更新图标 |
| `js/ui-populator.js` | 数据填充图标 |
| `js/renderers/reasoning-renderer.js` | 推理链展开/折叠图标 |
| `js/renderers/code-block-utils.js` | 代码块工具图标 |
| `js/modals/base.js` | 弹窗基础图标 |
| `js/modals/worldbook-modal.js` | 世界书操作图标 |
| `js/db-settings.js` | 数据库设置图标 |
| `js/tools-manager.js` | 工具管理图标 |

### 🔵 Draw 页面（独立体系）

`draw.html` 使用 Vue3 + TailwindCSS，图标已经是 stroke 风格，与 Lucide 兼容。**无需修改**。

---

## 四、新增图标操作步骤

1. 前往 [Lucide 图标库](https://lucide.dev/icons) 搜索所需图标
2. 点击图标 → 复制 SVG 代码
3. 修改 `width` 和 `height` 为对应场景尺寸（参见上方尺寸规范表）
4. 确保包含标准属性：`fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"`
5. 使用 `currentColor` 确保图标颜色跟随父元素的 `color` 属性

### ❌ 禁止行为

- **不要**使用 iconfont 生成的 SVG（`viewBox="0 0 1024 1024"`，path 数据巨大）
- **不要**使用 `fill="currentColor"` 的实心图标（Bootstrap Icons 风格）
- **不要**引入 Font Awesome 等字体图标库
- **不要**使用 emoji 替代图标（如 📋、🔧 等）
- **不要**在 SVG 中使用 `class="icon"` 或 `p-id` 等 iconfont 遗留属性
