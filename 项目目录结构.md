# 项目目录结构文档

> 生成时间：2026-05-13  
> 项目：ruthless-ai-assistant（无情AI助手）  
> 技术栈：HTML5 + ES6+ JavaScript + 原生CSS3

---

## 根目录

```
ruthless-ai-assistant/
├── index.html              # 主页面入口，聊天界面主框架
├── modals.html             # 所有弹窗模态框的HTML模板集合（通过fetch动态加载）
├── sidebar.html            # 侧边栏HTML模板（历史记录、会话列表等）
├── draw.html               # 独立的绘图/思维导图页面（集成 Mermaid + Vue）
├── favorite.ico            # 网站图标
├── GEMINI.md               # AI助手（乌鸦）行为规范与项目开发规则文档
├── CLAUDE.md               # AI助手（小鸡）Claude Code专用版行为规范与项目增量规则
├── README.md               # 项目总览说明，包含所有文件的简要介绍
├── start.bat               # Windows 启动脚本，快速启动本地服务
├── get-ip.bat              # 获取本机IP地址的批处理脚本（内网部署辅助）
├── release.ps1             # Release PowerShell脚本（更新版本号、缓存破坏）
├── release版本增加.bat       # Release脚本启动器（调用release.ps1）
├── 后端文件解析接口开发文档.md  # 后端文件解析接口开发文档（Spring Boot文件内容提取）
├── 图标使用指南.md           # 图标使用指南（Lucide/Feather风格SVG图标规范）
├── 预设角色导入包.json        # 预设角色导入包（可导入的AI角色配置集合）
└── 项目目录结构.md           # 本文件，项目目录结构总览
```

---

## css/ — 样式模块目录

> 所有样式按功能拆分，遵循组件化原则，通过 `index.html` 依次引入。

| 文件 | 说明 |
|------|------|
| `base.css` | 全局基础样式，CSS变量定义（颜色、字体、间距等设计令牌） |
| `layout.css` | 页面整体布局结构（顶部栏、主内容区、侧边栏的分区定位） |
| `theme.css` | 主题色系定义，支持多套主题切换（亮/暗色及自定义色系） |
| `appearance.css` | 外观个性化设置相关样式（字体大小、气泡样式、背景等） |
| `chat.css` | 聊天主区域样式（消息气泡、输入框、滚动区域） |
| `components.css` | 通用UI组件样式（按钮、标签、开关、头像、Toast通知等）*最大文件* |
| `modals.css` | 所有弹窗/模态框的通用样式 |
| `mcp.css` | MCP工具调用卡片、工具列表、工具结果展示区域样式 |
| `code-preview.css` | 代码预览侧边栏样式（代码展示、拖拽调宽等） |
| `responsive.css` | 响应式适配样式（移动端、窄屏布局调整） |
| `shadow-content.css` | Shadow DOM 内部内容样式（用于隔离样式作用域的场景） |
| `tools-menu.css` | 工具菜单浮层（输入框旁的工具选择菜单）样式 |

---

## js/ — 脚本模块目录

> 核心业务逻辑层，按功能高度模块化拆分。

### js/ 顶层文件

| 文件 | 说明 |
|------|------|
| `main.js` | 应用入口，初始化所有模块、绑定全局事件监听、协调各模块启动顺序 |
| `state.js` | 全局状态管理（当前会话、配置、运行状态等共享变量） |
| `renderer.js` | 核心渲染调度器，协调消息渲染流程（Markdown/代码/思维链等） |
| `utils.js` | 通用工具函数集合（防抖、深拷贝、格式化、DOM辅助等）*最大工具库* |
| `dom.js` | DOM操作封装层（元素查询、创建、属性操作等统一接口） |
| `events.js` | 全局事件常量定义（事件名枚举，避免硬编码字符串） |
| `api.js` | API基础配置（基础URL、请求头等全局API参数） |
| `api-common.js` | 通用API请求封装（统一的fetch请求、错误处理、重试机制） |
| `regex.js` | 项目所有正则表达式的集中管理文件（统一注册，避免散落各处） |
| `regex-engine.js` | 正则引擎，负责对消息内容进行正则规则匹配和替换处理 |
| `theme.js` | 主题切换逻辑（读取配置、动态切换CSS变量） |
| `appearance.js` | 外观设置逻辑（字体、背景、气泡样式等用户偏好应用） |
| `sidebar.js` | 左侧边栏逻辑（历史会话列表、新建/删除/切换会话） |
| `scroll-manager.js` | 滚动与视图锁定管理（流式输出自动滚动、用户滚动锁定机制） |
| `floor-jump.js` | 楼层跳转功能（多轮对话中快速跳转到指定消息楼层） |
| `message-manager.js` | 消息管理器（消息的增删改、隐藏、折叠、摘要等操作） |
| `chat-events.js` | 聊天区域事件处理（发送消息、重试、复制、点赞等用户交互） |
| `ui-events.js` | 通用UI事件处理（顶部栏、设置按钮、侧边栏切换等） |
| `ui-updater.js` | UI状态更新器（根据状态变化刷新界面显示，如发送按钮状态） |
| `ui-populator.js` | UI数据填充（将配置/数据回填到表单和界面元素） |
| `modal-events.js` | 弹窗事件逻辑总入口（各弹窗的打开/关闭/提交等事件绑定） |
| `modals.js` | 弹窗模块加载入口（汇聚各弹窗子模块的初始化） |
| `file-events.js` | 文件相关事件（附件上传、图片预览、文件类型校验等） |
| `attachment.js` | 附件处理逻辑（附件的存储、读取、展示及消息携带） |
| `settings-events.js` | 设置面板事件绑定入口（连接设置子模块） |
| `code-block-enhancer.js` | 代码块增强器（为代码块添加复制、语言标识、行号等功能） |
| `code-preview-manager.js` | 代码预览侧边栏管理（打开/关闭/同步代码内容/拖拽调宽） |
| `highlighter.js` | 代码高亮处理（调用highlight.js对代码块进行语法着色） |
| `custom-formatter.js` | 自定义格式化器（特殊内容格式化规则，如特定结构转换） |
| `quick-prompts.js` | 快捷提示词功能（预设提示词的展示、点击填充到输入框） |
| `chat-search.js` | 聊天内容搜索（在消息列表中关键字搜索并高亮定位） |
| `batch-delete.js` | 批量删除功能（多选消息进行批量操作） |
| `floating-button.js` | 浮动操作按钮（悬浮在聊天区的快捷功能按钮，如滚动到底部） |
| `draggable-list.js` | 可拖拽排序列表组件（通用拖拽排序逻辑封装） |
| `tools-manager.js` | 工具菜单管理（输入框工具按钮的显示/隐藏和功能分发） |
| `db.js` | 数据库（IndexedDB）操作封装（本地数据持久化的统一接口） |
| `db-settings.js` | 设置数据持久化（将配置项保存到本地数据库） |
| `db-choose.js` | 数据库选择弹窗逻辑（多数据库配置切换UI） |
| `db-table-choose.js` | 数据库表结构选择器（选择表并将结构注入提示词） |
| `ls-space.js` | LocalStorage 命名空间封装（统一前缀，避免键名冲突） |
| `purify.min.js` | DOMPurify库本地副本（XSS防护，对HTML内容进行净化） |
| `mcp-core.js` | MCP协议核心层（MCP连接、基础通信协议实现） |
| `mcp-management.js` | MCP工具/服务管理（MCP配置的增删改查和状态维护） |
| `mcp-renderer.js` | MCP工具调用结果渲染（将工具返回数据渲染为可视化卡片）*最大文件* |
| `mcp-session-manager.js` | MCP会话管理（工具调用的会话状态跟踪和生命周期管理） |
| `mcp-template-engine.js` | MCP模板引擎（动态渲染MCP工具配置和调用模板） |
| `mcp-tools-registry.js` | MCP工具注册表（已注册工具的元信息维护和查询） |
| `mcp-tools-selector.js` | MCP工具选择器UI（工具列表展示和用户选择交互） |

---

### js/services/ — 服务层

> 与外部通信的核心服务，负责AI请求和事件总线。

| 文件 | 说明 |
|------|------|
| `llm-service.js` | LLM服务核心（流式/非流式AI请求发送、响应解析、错误处理）*最重要文件* |
| `mcp-handler.js` | MCP工具调用处理器（拦截AI响应中的工具调用并分发执行） |
| `model-fetcher.js` | 模型列表拉取（从API获取可用模型列表并填充选择框） |
| `event-bus.js` | 事件总线（发布/订阅模式，模块间解耦通信的中间件） |
| `file-parser.js` | 文件解析服务（将Word/PDF/Excel等二进制文件解析为纯文本，供附件系统使用） |

---

### js/renderers/ — 渲染子模块

> 消息渲染的细粒度分工，各司其职。

| 文件 | 说明 |
|------|------|
| `markdown-engine.js` | Markdown渲染引擎（调用marked.js将Markdown转为HTML） |
| `markdown-worker-client.js` | Markdown Worker客户端（与Worker线程通信，非阻塞渲染） |
| `stream-renderer.js` | 流式渲染处理器（处理SSE流式输出的逐字追加渲染逻辑） |
| `reasoning-renderer.js` | 思维链渲染（AI推理过程`<think>`标签内容的折叠展示） |
| `code-block-utils.js` | 代码块工具函数（代码块识别、提取、格式化辅助方法） |
| `regex-utils.js` | 渲染用正则工具（渲染阶段的正则匹配辅助方法） |
| `style-manager.js` | 渲染样式管理（动态注入消息渲染所需的样式规则） |

---

### js/modals/ — 弹窗子模块

> 各功能弹窗的独立逻辑封装。

| 文件 | 说明 |
|------|------|
| `base.js` | 弹窗基类/公共方法（弹窗打开/关闭动画、遮罩层控制等共用逻辑） |
| `api-modal.js` | API配置弹窗（配置AI接口地址、Key等参数的弹窗逻辑） |
| `avatar-modal.js` | 头像设置弹窗（用户/AI头像上传、裁剪、内存优化管理） |
| `persona-modal.js` | 人格设置弹窗（AI角色扮演系统提示词配置） |
| `settings-modal.js` | 通用设置弹窗（各类参数配置的弹窗容器逻辑） |
| `message-editor-modal.js` | 消息编辑弹窗（对已发消息进行二次编辑的弹窗） |
| `worldbook-modal.js` | 世界书弹窗（知识库/背景设定条目管理，最大弹窗模块） |
| `quick-hide-modal.js` | 快速隐藏弹窗（批量隐藏/摘要相关配置弹窗） |
| `regex-modal.js` | 正则规则弹窗（自定义正则替换规则的配置和管理） |

---

### js/settings/ — 设置子模块

> 设置面板各功能区的独立逻辑。

| 文件 | 说明 |
|------|------|
| `api-settings.js` | API设置（多API配置管理、Key填写、模型选择等） |
| `mcp-settings.js` | MCP设置（MCP服务器的添加、配置、启用/禁用） |
| `persona-settings.js` | 人格/角色设置（系统提示词模板管理和切换） |
| `quick-prompt-settings.js` | 快捷提示词设置（自定义快捷指令的增删改排序） |
| `regex-settings.js` | 正则设置（全局正则替换规则的配置和管理） |
| `worldbook-settings.js` | 世界书设置（知识库条目的完整CRUD及匹配策略配置） |

---

### js/workers/ — Web Worker

| 文件 | 说明 |
|------|------|
| `markdown-worker.js` | Markdown渲染Worker（在独立线程中执行重型Markdown解析，避免阻塞主线程） |

---

## libs/ — 第三方库（本地化部署）

> 所有依赖本地化，无需CDN，保障内网环境运行。

| 文件 | 说明 |
|------|------|
| `marked.min.js` | Markdown解析库（将Markdown文本转换为HTML） |
| `highlight.min.js` | 代码语法高亮库（支持多种编程语言着色） |
| `atom-one-dark.min.css` | highlight.js 暗色主题样式 |
| `atom-one-light.min.css` | highlight.js 亮色主题样式 |
| `echarts.min.js` | Apache ECharts图表库（用于MCP工具返回数据的图表可视化） |
| `cropper.min.js` | 图片裁剪库（头像上传时的裁剪功能） |
| `cropper.min.css` | cropper.js 配套样式 |
| `mammoth.browser.min.js` | Word(.docx)文档解析库（将docx文件提取为纯文本/HTML） |
| `xlsx.full.min.js` | Excel解析库（SheetJS，解析.xlsx/.xls电子表格文件） |
| `pdf.min.js` | PDF解析库（将PDF文件提取为文本内容） |
| `pdf.worker.min.js` | PDF Worker（PDF解析的Web Worker线程脚本） |
| `jszip.min.js` | ZIP压缩库（PDF等基于ZIP格式的文件解析依赖） |

---

## draw_css/ — 绘图页面专用资源

> 仅供 `draw.html` 使用的独立资源，避免污染主项目依赖。

| 文件 | 说明 |
|------|------|
| `mermaid.min.js` | Mermaid图表库（流程图、时序图、ER图等渲染） |
| `vue.global.js` | Vue 3 框架（绘图页面使用Vue构建交互界面） |
| `tailwindcss.3.4.17.js` | Tailwind CSS（绘图页面的原子化CSS工具） |
| `css2.css` | 绘图页面补充字体/样式 |

---

## font/ — 字体文件

| 文件 | 说明 |
|------|------|
| `PingFangSC_0.ttf` | 苹方字体（保障跨平台字体显示一致性，解决Windows中文渲染差异） |

---

## clearDB/ — 本地数据库修复工具

> 用于排查和修复 IndexedDB 数据异常的独立工具页。

| 文件 | 说明 |
|------|------|
| `fix-localStorage-tool.html` | 可视化数据库修复工具页面（查看/清除/修复本地存储数据） |
| `fix-localstorage.js` | 数据库修复脚本（提供批量清理和数据迁移辅助函数） |

---

## mcp导入模板/ — MCP工具配置模板

> 预置的MCP工具配置JSON模板，可直接导入使用。

| 文件 | 说明 |
|------|------|
| `全国常用电话工具_V3.json` | MCP工具模板：全国常用电话查询工具 |
| `综合新闻工具_V4.json` | MCP工具模板：新闻资讯聚合查询工具 |
| `网页源码获取工具_V1.json` | MCP工具模板：网页HTML源码抓取工具 |
| `藏头诗工具_新模板.json` | MCP工具模板：藏头诗生成工具 |

---

## 功能说明/ — 核心模块设计文档

> **开发必读**：修改相关模块前务必先查阅对应文档，了解设计背景。

| 文件 | 覆盖范围 |
|------|------|
| `功能说明-滚动与视图锁定.md` | `ScrollManager` 设计原理、自动滚动与用户锁定机制 |
| `功能说明-消息渲染与发送全流程.md` | 从用户输入到AI响应渲染的完整链路说明 |
| `功能说明-代码预览侧边栏.md` | 代码预览侧边栏打开/关闭/同步/拖拽调宽机制 |
| `功能说明-正则表达式.md` | `regex.js` 中所有正则的集中说明 |
| `功能说明-流式请求开关与调试优化.md` | 流式/非流式切换、调试模式逻辑说明 |
| `功能说明-隐藏与总结.md` | 消息折叠、隐藏摘要、懒加载等机制 |
| `功能说明-通知提示系统.md` | 全局Toast通知系统的使用和扩展方式 |
| `mcp请求到渲染全流程.md` | MCP工具调用从请求到渲染的完整链路 |
| `头像内存优化说明.md` | 头像加载内存优化策略和缓存机制 |
| `360浏览器布局偏移修复方案.md` | 360浏览器兼容性布局偏移问题的修复方案记录 |
| `TODO_渲染引擎优化_2026-01-20.md` | 渲染引擎优化任务清单（历史记录） |

---

## 模块依赖关系概览

```
index.html
  ├── css/（所有样式）
  ├── libs/（第三方库）
  ├── js/main.js（入口）
  │     ├── state.js（全局状态）
  │     ├── db.js（本地数据库）
  │     ├── services/llm-service.js（AI请求核心）
  │     ├── services/event-bus.js（事件总线）
  │     ├── services/file-parser.js（文件解析服务）
  │     ├── renderer.js → renderers/*（渲染链）
  │     ├── mcp-core.js → mcp-*.js（MCP工具链）
  │     ├── sidebar.js（侧边栏）
  │     ├── chat-events.js（聊天交互）
  │     ├── settings-events.js → settings/*（设置面板）
  │     ├── modal-events.js → modals/*（弹窗系统）
  │     └── utils.js / dom.js（基础工具）
modals.html（弹窗模板，动态加载）
sidebar.html（侧边栏模板，动态加载）
draw.html（独立绘图页）
```

---

*文档由 乌鸦 自动生成，如项目有新增文件请同步更新本文档。*
