# 智能摸鱼 (Ruthless AI Assistant) — 更新日志

本文档记录 **智能摸鱼** 项目的每一次重大更新、功能迭代与缺陷修复。

---

## 📅 2026-08-25 (v260825)

### ✨ 新增与重构功能

1. **楼层快速跳转系统 (v2.0 架构级重构)**：
   - **单/多分支自适应识别**：全面兼容单分支（未分叉）与多分支环境，浮窗顶部自动展示当前激活分支的版本标签（如 `[分支 1/3]`）；
   - **楼层号极速直达工具栏**：浮窗内新增输入框与“跳转”按钮，支持直接输入 `#楼层号` 快速定位；
   - **懒加载智能穿透展开**：点击未展开的折叠历史楼层时，系统自动联动渲染器全量补录或精准展开目标占位卡片，平滑滚动高亮；
   - **多模态类型安全防御**：重构摘要提取器，完美兼容纯文本、多模态图片数组、附件对象，杜绝任何类型报错；
   - **生命周期联动自动关闭**：切换会话、切换分支或按下 `ESC` 键时，跳转浮窗自动平滑关闭。

2. **智能懒加载占位胶囊条升级 (Plan A)**：
   - **单行紧凑胶囊条设计**：彻底告别原本沉闷的空方块占位符，升级为横向自适应单行紧凑胶囊条；
   - **AI 正文优先提取算法**：智能识别并剥离 `<think>...</think>` / `<thinking>...</thinking>` 深度思考标签，优先呈现干净的回答正文摘要；若仅有思考过程则友好标注 `[思考] 思考内容...`；
   - **整卡任意位置一键展开**：无需费力瞄准小按钮，点击胶囊条任意区域立即无缝展开为完整消息，悬停带有丝滑高亮微动效；
   - **首屏超低内存与零跳动保证**：1000 楼长会话首屏仅全量渲染最近 5~10 楼，其余以单行超轻量 DOM 占位（0 Markdown 解析、0 Worker 消耗），且无视口高度跳动。

---

### 🐛 缺陷修复与性能加固

1. **根治主启动链路死锁（Promise 永久挂起阻断启动流）**：
   - **原因**：在 [`main.js`](js/main.js) 执行 `await switchToConversation(null)` 时，[`renderer.js`](js/renderer.js) 在空会话欢迎语分支直接 `return` 漏掉了 `if (callback) callback();`，导致外部 Promise 永久挂起，阻塞了位于其后的 `initFloorJump()` 等所有模块初始化；
   - **修复**：在欢迎语分支补齐回调调用闭环，并将所有核心基础设施模块（楼层跳转、悬浮折叠按钮）提前至 `initDom()` 之后第一时间初始化，解除视图异步依赖。
2. **修复悬浮跳转按钮点击穿透与层级遮挡**：
   - 为 `#jump-to-floor-btn` 强制设置 `pointer-events: auto !important;`，所有子元素设置 `pointer-events: none !important;`，杜绝点击事件被父类阻断；
   - 将 `#floor-jump-panel` 层级提升至 `z-index: 1000 !important;`，微调坐标防止被底部输入框遮挡。
3. **消除历史长消息回看滚动跳动（Scroll Jumping）**：
   - 彻底移除了 `content-visibility: auto` 在高度极端离散的 AI 复杂富文本场景下的突变副作用，恢复 100% 稳定的像素级丝滑滚动体验。

---

## 📅 2026-08-22

- **会话记忆与智能总结系统 3 大模式全面上线**（🔄 递归滚动记忆、📑 卡片流拼接记忆、⚔️ 跑团 TRPG 结构化双表）；
- **后台自动总结实时秒表与流式打字机同步**；
- **分叉重发因果时间线智能命中与一键回滚**；
- **单楼层/区间快速隐藏与 Token 动态感知徽章**。
