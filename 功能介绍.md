# 无情AI助手 — 功能介绍

> **你的 AI，你做主。** 一款纯前端、本地优先、高度可定制的 AI 聊天工作台。

---

## 这是什么？

**无情AI助手** 是一个运行在浏览器中的 AI 聊天界面，它不依赖任何后端服务，所有数据都存储在你的浏览器本地。你可以将它连接到任何兼容 OpenAI 格式的 API，打造专属于你的 AI 对话体验。

**一句话概括**：如果你觉得市面上的 AI 聊天工具要么功能太简陋、要么隐私没保障、要么定制能力不够——那你来对地方了。

---

## 核心亮点

### 本地优先，隐私无忧

- 所有会话记录、API 密钥、角色配置都存储在浏览器本地（IndexedDB + LocalStorage）
- 不上传任何数据到第三方服务器
- 内网环境也可正常部署使用，所有第三方库均已本地化

### 高度可定制

- **9 种主题配色**：深色、浅色、绿色、金色、粉色、蓝色、紫色、橙色、灰色，总有一款适合你
- **外观精细调整**：字体大小、气泡样式、引用色、斜体色、下划线色……每个细节都可微调
- **头像管理**：自定义用户/AI 头像，支持裁剪，每个会话还能单独设置专属头像

### 高性能渲染

- **Web Worker 架构**：Markdown 解析在独立线程完成，主线程永不卡顿
- **流式输出**：支持 SSE 流式响应，AI 回复逐字呈现
- **智能滚动**：自动跟随 + 用户滚动锁定，想看哪里看哪里
- **增量更新**：流式输出时原地更新 DOM，不闪烁、不跳变

---

## 功能全览

### 智能聊天

| 功能 | 说明 |
|------|------|
| 实时 Markdown 渲染 | 支持 GFM 标准：表格、列表、任务列表、删除线等，所见即所得 |
| 代码高亮 | 基于 highlight.js，支持数十种编程语言语法着色 |
| 代码预览侧边栏 | 代码块自动展开到右侧边栏，可拖拽调宽，方便阅读长代码 |
| 思维链展示 | AI 推理过程独立折叠展示，支持 DeepSeek 等模型的思考链 |
| 双视图切换 | 任意消息可在「渲染视图」和「纯文本视图」之间一键切换 |
| 全屏模式 | 沉浸式全屏聊天，专注于对话本身 |
| 消息搜索 | 在当前会话内搜索关键字，快速定位历史消息 |
| 楼层跳转 | 输入楼层号一键跳转，长对话不再迷路 |

### 会话管理

| 功能 | 说明 |
|------|------|
| 本地历史记录 | 所有会话安全存储在 IndexedDB，随时回顾 |
| 会话分支 | 从任意一条消息创建新分支，探索不同对话走向，互不干扰 |
| 分支导航 | 顶栏分支指示器，左右切换不同版本，像 Git 一样管理对话 |
| 会话置顶 | 重要会话置顶，快速访问 |
| 批量删除 | 多选会话批量操作，清理效率翻倍 |
| 隐藏与总结 | 将早期对话标记为隐藏，用 AI 生成摘要替代，节省上下文 Token |

### 多 API 管理

| 功能 | 说明 |
|------|------|
| 多端点配置 | 添加多个 API 端点，一键切换，兼容所有 OpenAI 格式 API |
| 模型自动拉取 | 自动从 API 获取可用模型列表，无需手动输入 |
| 流式/非流式切换 | 根据需要选择流式输出或一次性返回完整响应 |
| 模型参数调节 | 温度、Top-P、Top-K、Min-P、最大 Token 数等参数精细调节 |
| 调试模式 | 启用后在网络面板显示完整响应数据，排查问题利器 |

### 角色系统（Persona）

| 功能 | 说明 |
|------|------|
| 多角色管理 | 创建、保存、切换不同系统提示词，快速改变 AI 身份和行为 |
| 角色导入/导出 | 一键导入/导出角色配置，分享给其他人 |
| 角色选择器 | 顶栏下拉快速切换角色，无需进入设置 |
| 预设角色包 | 内置预设角色导入包，开箱即用 |

### 备忘录（World Book）

| 功能 | 说明 |
|------|------|
| 持久化知识库 | 常用背景资料、代码片段、个人信息存入其中 |
| 按需注入 | 可选择性地让内容在每次请求时自动附加到上下文 |
| 按会话绑定 | 不同会话可以启用不同的备忘录条目 |
| 深度控制 | 精确控制每个条目的注入深度和触发条件 |

### 双阶段正则引擎

| 功能 | 说明 |
|------|------|
| 请求前处理 | 在发送给 API 前，对消息内容进行正则替换 |
| 响应后处理 | 在渲染到界面后，对 AI 回复进行正则替换 |
| 灵活语法 | 支持 `/pattern/flags` 字面量格式和纯字符串格式 |
| 典型用例 | 自动脱敏、口吻转换、内容过滤、格式美化……只有想不到，没有换不了 |

### 文件附件与解析

| 功能 | 说明 |
|------|------|
| 文本附件 | 支持上传 txt、js、py、html、css、json、md 等文本文件 |
| 图片附件 | 支持上传 png、jpg、gif、webp 等图片 |
| 文档解析 | **支持 Word(.docx)、PDF、Excel(.xlsx/.xls) 等文档自动解析为文本** |
| 国产格式兼容 | 支持 WPS 格式（.wps、.et）和国产 PDF 格式 |
| 数据库注入 | 选择数据库表结构并注入到提示词，让 AI 理解你的数据模型 |

### MCP 工具调用系统

这是无情AI助手最具特色的功能之一，让 AI 不再只会"说"，还能"做"。

| 功能 | 说明 |
|------|------|
| 工具调用 | AI 根据对话内容自主决定调用预定义工具（天气、汇率、新闻等） |
| 自定义工具 | 通过图形界面创建工具，对接任何 HTTP API，无需写代码 |
| 智能渲染 | 工具结果以结构化卡片呈现，而不是原始 JSON |
| ECharts 图表 | 工具返回的数据可自动渲染为图表可视化 |
| Mermaid 图表 | 支持 Mermaid 语法渲染流程图、时序图、ER 图等 |
| 模板引擎 | 内置智能模板引擎，自动适配 API 返回格式生成美观卡片 |
| 多轮调用 | AI 可在单次对话中进行最多 10 轮工具调用链 |
| 并发管理 | 支持并发执行多个工具调用，带完善的超时和错误处理 |
| 工具导入/导出 | 预置 MCP 工具模板，一键导入即可使用 |

### 绘图与可视化

| 功能 | 说明 |
|------|------|
| Mermaid 流程图 | 独立绘图页面，支持流程图、时序图、ER 图、甘特图等 |
| Vue 3 交互界面 | 绘图页面基于 Vue 3 构建，操作流畅 |
| 实时预览 | 编写 Mermaid 代码实时渲染预览 |

### 数据管理

| 功能 | 说明 |
|------|------|
| 配置导入/导出 | 一键导出所有配置（API、角色、正则、备忘录等）为 JSON |
| 会话导入/导出 | 单个或全部会话导出为 JSON，随时备份恢复 |
| 数据修复工具 | 内置 IndexedDB 修复工具页面，数据异常时自助修复 |
| 版本发布脚本 | 自动更新版本号和缓存参数，确保用户始终加载最新版 |

---

## 技术特点

| 特点 | 说明 |
|------|------|
| 纯前端架构 | HTML5 + ES6+ JavaScript + 原生 CSS3，无构建依赖 |
| 零框架主应用 | 主聊天界面不依赖任何前端框架，极致轻量 |
| 模块化设计 | 60+ 个 JS 模块，功能高度解耦，易于维护和扩展 |
| 本地化部署 | 所有第三方库本地化，无需 CDN，保障内网环境运行 |
| XSS 防护 | DOMPurify 全局净化，安全性拉满 |
| 事件总线 | 发布/订阅模式解耦模块间通信 |
| 竞态锁机制 | 异步渲染管线内置 renderId 锁，防止 DOM 混乱 |

---

## 快速开始

### 环境要求

- 现代浏览器（Chrome / Edge / Firefox / Safari）
- [Node.js](https://nodejs.org/)（用于启动本地 HTTP 服务器）

### 安装与启动

**方式一：命令行启动**

```bash
# 1. 全局安装轻量级 HTTP 服务器
npm install -g http-server

# 2. 进入项目目录
cd ruthless-ai-assistant

# 3. 启动服务
http-server -p 8081 -o
```

**方式二：双击启动（Windows 用户）**

直接双击项目根目录下的 `start.bat` 即可。

### 配置 API

1. 打开应用后，点击顶栏的设置按钮
2. 在 API 设置中填入你的 API 地址和密钥
3. 选择模型，开始聊天

---

## 适用场景

- **个人 AI 助手**：连接自己的 API，打造专属 AI 对话工具
- **内网部署**：企业内网无法访问外网，本地化库 + 纯前端完美适配
- **AI 开发调试**：调试模式、正则引擎、工具调用……开发者的瑞士军刀
- **角色扮演**：Persona 系统 + World Book，沉浸式角色对话
- **知识管理**：备忘录 + 文档解析 + 数据库注入，让 AI 理解你的领域
- **团队协作**：导出/导入配置和工具，团队共享最佳实践

---

## 与同类工具的区别

| 对比维度 | 无情AI助手 | 通用 AI 网页 | 其他开源前端 |
|----------|-----------|-------------|-------------|
| 数据存储 | 100% 本地 | 云端 | 部分本地 |
| 内网部署 | 原生支持 | 不支持 | 部分支持 |
| 多 API 管理 | 支持 | 固定 | 部分支持 |
| 角色系统 | 完整 | 无 | 部分支持 |
| 工具调用（MCP） | 完整 | 无 | 简单 |
| 正则引擎 | 双阶段 | 无 | 无 |
| 文档解析 | Word/PDF/Excel | 无 | 无 |
| 主题定制 | 9 主题 + 微调 | 固定 | 2-3 主题 |
| 构建依赖 | 无 | 无 | Webpack/Vite |

---

## 项目信息

- **技术栈**：HTML5 + ES6+ JavaScript + 原生 CSS3
- **代码规模**：60+ 个 JS 模块、12 个 CSS 模块
- **开源协议**：详见项目根目录
- **版本**：v260731

---

*想了解更多技术细节？查看 `功能说明/` 目录下的开发文档。*
